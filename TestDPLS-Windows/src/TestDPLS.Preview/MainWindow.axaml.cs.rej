--- src/TestDPLS.Preview/MainWindow.axaml.cs
+++ src/TestDPLS.Preview/MainWindow.axaml.cs
@@ -380,7 +380,7 @@ public partial class MainWindow : Window
 
         var submit = Primary(S.Initialized ? "Подключиться" : "Сохранить", () =>
         {
-            if (S.Initialized) _client.Authenticate(pass.Text ?? "");
+            if (S.Initialized) _client.Authenticate(S.SetupPassword ?? "");
             else _client.Setup(nameBox?.Text ?? S.SetupName, pass.Text ?? "");
         });
         DockPanel.SetDock(submit, Dock.Bottom);
